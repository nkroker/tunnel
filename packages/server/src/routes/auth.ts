import { Router } from 'express';

const router: Router = Router();

// Add authentication-related routes here
// Example:
// router.post('/login', login);
// router.post('/register', register);

export default router;
