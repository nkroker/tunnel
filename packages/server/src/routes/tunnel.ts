import { Router } from 'express';

const router: Router = Router();

// Add tunnel-related routes here
// Example:
// router.post('/create', createTunnel);
// router.get('/:id', getTunnel);

export default router;
