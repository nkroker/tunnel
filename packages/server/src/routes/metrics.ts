import { Router } from 'express';

const router: Router = Router();

// Add metrics-related routes here
// Example:
// router.get('/', getMetrics);
// router.get('/:tunnelId', getTunnelMetrics);

export default router;
